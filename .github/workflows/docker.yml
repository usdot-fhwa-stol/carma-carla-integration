name: Docker build

on:
  pull_request:
      types: [fix_CI_builds, opened, synchronize, reopened]

jobs:
  docker:
    uses: usdot-fhwa-stol/actions/.github/workflows/docker.yml@main
    with:
      tag_name_suffix: noetic
